define.extend('raptor/i18n', function(require, exports, module) {
    'use strict';
    //var vm = require('vm');
    return {
        /*
        _eval: function(code) {
            var sandbox = {
                require: require
            };

            vm.runInNewContext(code, sandbox);
        }
        */
    };
});