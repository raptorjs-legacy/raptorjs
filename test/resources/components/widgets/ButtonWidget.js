define.Class(
    'components.widgets.ButtonWidget',
    function(require) {
        return {
            init: function(config) {
                console.error('Button initialized!', config);
            }
        };
    }
);